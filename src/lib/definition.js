class Definition {
    constructor({ type, version, keyProperties }) {
        this.type = type
        this.version = version
        this.keyProperties = keyProperties
    }
}

module.exports = Definition